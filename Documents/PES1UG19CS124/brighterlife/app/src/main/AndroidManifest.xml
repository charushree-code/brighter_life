<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brighterlife">

    <uses-permission android:name="andriod.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Brighterlife">
        <activity android:name=".breathe.breathe_activity"></activity>
        <activity
            android:name=".splash"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.Register" />
        <activity android:name=".auth.Login" />
        <activity
            android:name=".page.editPage"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".page.addnotes"
            android:label="@string/title_activity_addnotes"
            android:theme="@style/Theme.Brighterlife.NoActionBar" />
        <activity
            android:name=".page.journal"
            android:label="@string/title_activity_journal"
            android:theme="@style/Theme.Brighterlife.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.NoActionBar"></activity>
    </application>

</manifest>